# SEPA cockpit runtime — pins mirror the working laptop env (pip show), not
# environment.yml (whose pandas pin has drifted from what is installed).
streamlit==1.58.0
pandas==2.2.3
numpy==2.2.5
plotly==5.24.1
pyarrow==23.0.1
yfinance==0.2.65
curl-cffi==0.13.0
requests==2.32.5
beautifulsoup4==4.13.4
alpaca-py==0.43.4
python-dotenv==1.2.2
