# Weekend-hunt pipeline: the deterministic side of the Sunday Step-3 review.
# The /weekend-hunt skill (Claude) judges the charts and writes verdicts through
# pipeline.append_verdicts; everything else lives here.
